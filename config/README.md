# config/

Generator configuration files. Will contain:
- Synthea configuration (if using Synthea)
- Custom generator parameters (patient count, date ranges, condition distributions)
- Locale/region settings (zip codes, area codes, name pools)
- Output format settings
